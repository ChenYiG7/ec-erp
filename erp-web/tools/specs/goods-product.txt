# 商品库列表 spec(gen:page 拍板表;行内 # 后为注释,# 开头整行注释)
# 示范②:readonly 只读 + TreeFilter 分类过滤;后端 POST /api/goods/products(复杂建档)刻意不开放,新增随 add-domain 后续拍板
module=goods
domain=product
base=goods/products
entity=Product
nameZh=商品管理
permPrefix=goods
todoId=16
menuParent=6
path=/goods/product
component=goods/product/index
icon=component
readonly=true

# keyword 只在 ProductQuery(搜索),不在 Response —— 核账警告属预期
field=keyword    role=search label=关键字
field=spuCode    role=column label=SPU编码  width=180
field=name       role=column label=商品名称 width=220
field=brandId    role=column label=品牌ID   width=100
field=categoryId role=column label=分类ID   width=100
field=status     role=column label=状态     enum=1:启用:success|0:停用:danger  width=90
field=createdAt  role=column label=创建时间 width=170

# 页面级槽位:分类过滤由左侧 TreeFilter 驱动 initParam.categoryId(人工接线,生成器不管树)
todo=sku:SKU 明细联动(ProductSkuResponse /api/goods/skus)与平台商品绑定,后续迭代补
