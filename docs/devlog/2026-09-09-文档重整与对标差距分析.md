# 文档重整与对标差距分析(docs/10 立项)

- 日期: 2026-09-09
- 收尾提交: (本次会话产出,随下次提交入库)

## 拍板

- 对标结论从 TODO.md #17 与 docs/02 §14 两个散落点**收口为独立文档 `docs/10-对标差距分析.md`**:五个对标系统(开源三家 + 领星/优麦云)的分域功能矩阵、G1~G10 差距清单(按业务影响排序)、待办优先级(P0~P3)与「已拍板不做」清单统一收录;TODO.md 与 docs/02 只留结论引用,避免三处漂移。
- TODO.md 结构重整:**第一节=当前待办(按 P0~P3 优先级,与 docs/10 §5 同源)/第二节=已完成编号索引(压缩为结论+关键拍板)/第三节=红线与坑**。压缩依据:实施细节已在 devlog 31 篇归档与代码注释,重整前全文可从 git 历史追溯;编号 #1~#23 全部保留(代码 `TODO(编号)` 注释依赖)。
- docs/08 新增 2 条(KbVectorIndex 派生索引可重建 / ShipmentSyncService AFTER_COMMIT 回传编排,新组「事件驱动编排」),原 16 条 erp-codegen 顺延为 18;**条目 11/15 编号不动**(docs/05 与 devlog 存在外部引用,重排前必须先查引用);登记纪律总量上限 15→20。
- docs/02 分期总览表**新增「进度」列**,原一期规划的「订单审核」等未落地项显式标注 ❌ 并挂 TODO P2,消除文档规划与实际状态的暗差异。

## 改动

- 新增:`docs/10-对标差距分析.md`(调研快照 2026-09-09:wimoor/OmniTrade/qihang 按仓库与官网复核,含 qihang 已迁 Vue3 + Boot 4.1 的最新状态;领星/优麦云按官网功能宣传)。
- 重写:TODO.md(1438 行→约 190 行结构化)、README.md(功能总览状态更新 + 差距简述 + docs/10 链接)、CLAUDE.md(必读清单加 docs/10 与 08、目录速查全量刷新、技术栈表补 POI)。
- 更新:docs/01(模块表状态列 + RAG 选型口径修正)、docs/02(状态口径头注 + 四节状态修正 + 分期进度列)、docs/06(进度对账重写为阶段级结论)、docs/08(新增 2 条 + 编号顺延)。

## 坑

- Edit 工具改 docs/02 时先遇 old_string 编码不一致(全角括号 vs 半角)再遇 EBUSY 瞬时文件锁——**长文档批量编辑前先 Read 最新内容取精确串,失败重试即可,勿盲改**。
- 对标信息时效性:qihang 的技术栈记载(Vue2/Boot4.0)在项目内文档已过时(实际已迁 Vue3、升 Boot 4.1.0)——**对标库条目应随每次调研刷新,勿沿用旧快照**。

## 未尽

- docs/10 的功能矩阵为调研快照口径,对标系统迭代后(尤其 OmniTrade SaaS 化、qihang 企业版能力下沉)需定期刷新;建议随大版本对齐动作同步。
- TODO.md 压缩后,旧库手工 ALTER 清单只存 git 历史引用,若有第三方旧库用户需引导其查 `TODO.md@重整前` 版本。

## 二次重整(同日下午:TODO 瘦身 + docs/02 全景化)

### 拍板

- **文档职责再划分**:TODO.md = 纯待办清单(只含未完成项);**docs/02 = 系统功能全景**(规划 + 落地状态合一,状态列即当前事实)。上午定下的「待办/编号索引/红线」三节结构被本次取代——第二节(已完成编号索引 #1~#23,78 行)整体移除,信息去向三处:功能现状收编 docs/02 各分域状态列、实施细节在 devlog 归档、历史全文在 git。
- **编号约定**:#1~#23 历史编号**不复用、不重排**;代码内残留 `TODO(#3/#6/#10/#12/#17)` 注释为迭代槽位或化石标注(grep 校验:#10 采购实体/schema DDL 注释为已完成化石,#3/#6/#12/#17 余量均在待办分节有对应)。新 TODO 一律用新编号。

### 改动

- 重写:docs/02(139→176 行,全部 14 分域表格化并补状态列;§4 新增移动加权成本账行、§9 新增退款勾稽行、§11 报表六件展开、§5/§6/§8/§12 由文字段改表格;**顺手修正 §13 RAG 行——上轮重整漏改,仍标"未做",实已落地 V1**)。
- 重写:TODO.md(168→113 行,三节→两节:待办 + 红线与坑;头部补职责边界与编号约定声明)。
- 联动:CLAUDE.md(必读清单 TODO.md 行改纯待办口径 + 铁律 1 补"编号不复用")、README.md(待办链接去掉"第一节"并补 docs/02 指向;快速开始 ALTER 注释改为"已执行验证,git 历史存档")、devlog/00-现状快照.md(头部口径:功能全景以 docs/02 为准)。

### 坑

- 上轮重整的 7 处定点更新有一处实际未生效(docs/02 §13 RAG "未做"→"✅ 落地"),对照 devlog 拍板记录复核时才发现——**批量定点编辑后必须 grep 复核目标串,不能只信 Edit 成功回执**(Edit 成功≠改对了位置,可能 old_string 命中了另一处相似文本)。

