# 休眠模块地基第二批:6 表骨架经 erp-codegen 生成后按域角色裁剪——erp-shop 三域(pull_log

2026-09-03 休眠模块地基第二批:6 表骨架经 erp-codegen 生成后按域角色裁剪——erp-shop 三域(pull_log/shop_product/shop_product_sku,游标读取 `findLastSuccessWindowEnd` 已实现,人工绑定接口 `PUT /api/shop-product-skus/{id}/bind` 已实现:回填 sku_id + match_status=2 幂等)、erp-order 两域(shop_order/shop_order_item,子表仅 entity+mapper,Response 剔 raw_json)、erp-inventory 补 inventory_flow(流水只读)。InventoryService.change() 唯一入口落地(@Transactional 同事务写 flow,before/after 记 qty_available,自动建行,负库存拒绝)。erp-shop pom 新引 spring-security-core(仅 @PreAuthorize 注解,过滤链仍收口 erp-api);新域菜单种子未登记,随前端工程统一规划。Git Bash 跑 codegen 注意:MSYS 会把 `-Dpath=/api/...` 转成 Windows 路径,须前缀 `MSYS2_ARG_CONV_EXCL='-Dpath='`(勿用 MSYS_NO_PATHCONV=1,会破坏 mvn 自身 classpath)。
