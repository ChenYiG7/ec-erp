# 站内通知落地(#14,拍板:站内先行,邮件/短信/IM 即微信/飞书/钉钉/企微后续做):`sys_notificati

2026-09-04 站内通知落地(#14,拍板:站内先行,邮件/短信/IM 即微信/飞书/钉钉/企微后续做):`sys_notification` 表(系统写入对外只读+本人已读状态,`read_status` 列名规避 is_ 前缀);写侧唯一入口 `SysNotificationService.pushAllUsers`(@Transactional 扇出全部启用用户,content 截断 1000),对外 = 我的通知分页/详情(归属校验)/未读数/单条已读/全部已读,`SysUserService.listEnabledUserIds` 供扇出;拉单告警接线 = `PullConsts.FAILURE_ALERT_THRESHOLD=3` + `PullLogService.shouldAlertContinuousFailure` 无状态判定(pull_log 自身去重,恰达阈值轮次告警一次,成功重新计数),两拉单 Job catch 内接线、告警写失败不阻断;⚠️ 坑:`LambdaUpdateWrapper.set` 急切解析列名,纯 Mockito 单测环境无 MP 元数据会炸,更新值走实体 null-skip。TODO(#4/#5) 连续失败告警占位已收口。
