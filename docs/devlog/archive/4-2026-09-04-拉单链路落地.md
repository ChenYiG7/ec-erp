# 拉单链路落地(#4 主体,TODO.md #4 有全量细节):`ShopOrderService.saveUnified

2026-09-04 拉单链路落地(#4 主体,TODO.md #4 有全量细节):`ShopOrderService.saveUnifiedOrder` = 唯一写入口,主表 ON DUPLICATE KEY UPDATE upsert(XML,行别名语法 MySQL 8.0.19+)→ uk 反查 id → 明细先删后插同事务;`OrderPullJob`(erp-api/job)@Scheduled fixedDelay 15 分钟,窗口 = 上次成功 window_end 左叠 5 分钟、首次回溯 90 天(`erp.pull.*` 可配),单线程调度器 + 店铺级 Redisson 锁防重入(2026-09-04 #13 锁选型同日二次定版:一期即定型 Redisson,免二期灰度迁移窗口的进程内锁静默失效;`LockService` 收口非阻塞 tryAcquire + 看门狗续期,Redis 故障按 `erp.lock.fail-open` 降级——一期 true 无锁照跑靠 uk 幂等兜底、二期多实例置 false,docs/07 §1 ③),MDC traceId;`ShopProductSkuService.mapSellerSkuToSkuId`(XML JOIN)供编排批量翻译明细 sku_id;`ShopService.listEnabledShopIds` 供调度;`PullLogService.recordSuccess/recordFailure` 写入侧(失败不推游标)。**模块边界演进**:erp-order 新增 erp-platform-sdk 依赖,仅消费 unified 模型(docs/07 §2.2 已更新,后续 erp-goods/erp-aftersale 同规约);常量新增 erp-common `PullConsts`(dataType/pullWay/窗口重叠/时区)。平台 adapter(#3)接入前各店自动跳过。
