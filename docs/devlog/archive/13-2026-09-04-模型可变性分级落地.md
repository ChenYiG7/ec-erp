# 模型可变性分级落地(#13,docs/07 §1 新小节 + §12 反模式两行):评审"业务层坚决不用 set/Ent

2026-09-04 模型可变性分级落地(#13,docs/07 §1 新小节 + §12 反模式两行):评审"业务层坚决不用 set/Entity 按 JPA 保留 setter/DTO 能省则省"提案后修正定版——本项目 ORM 是 **MyBatis-Plus 非 JPA**;五档分级:Entity **默认 @Data+@Builder+双构造**(MP 框架依赖 setter:反射映射/updateById null-skip/Lambda 方法引用;@Builder 供纯构造装配位,读改写/写前回填仍 setter;setter 只许落白名单:toEntity/from 映射、写前回填、安全加工唯一入口、库存读改写、Service 内部 entity 装配、Page.setRecords)、Query 保持 class(record 不能继承 PageQuery,继承例外)、**Response/SaveRequest = record+@Builder**(from 用 builder 命名传参防相邻同类型字段错位;"构造后补字段"用 wither:`ShopOrderResponse.withItems`/`LoginResponse.withToken`;**含敏感字段必须手写 toString 脱敏**——@ToString.Exclude 在 record 上不生效:ShopSaveRequest/SysUserSaveRequest 已脱敏,存量 LoginRequest 等本就自带)、业务层"new+长串 set"装配一律 builder/构造器/record。erp-codegen 八件套同步改产 record;存量 32 个 API 模型脚本批量翻新 + 特例手工;Unified*/PullLog 加 @Builder(@Data+双构造保无参构造,MP/存量测试零破坏),AmazonOrderTranslator/PullLogService/AuthService 装配改 builder 链;CategoryNode 分类树节点 record 化;`mvn test` 13 模块全绿。
