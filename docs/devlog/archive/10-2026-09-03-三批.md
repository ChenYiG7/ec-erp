# 三批(二期单据):erp-codegen 升级 `parts=`(生成件子集)/`readOnly=`(系统写入表只读,

2026-09-03 三批(二期单据):erp-codegen 升级 `parts=`(生成件子集)/`readOnly=`(系统写入表只读,砍 SaveRequest+写侧)两参数;docs/03 §5 草案 6 表定稿进 01_schema_init.sql 并一次生成——supplier/purchase_order(全 CRUD)、purchase_order_item(entity+mapper)、purchase_inbound(全 CRUD)、delivery_order(全 CRUD)、aftersale_order(只读);状态机枚举均标"草案,业务确认后调整";采购/发货/售后业务规则 = TODO #10/#11/#12;三期表(settlement/ad_report/ai_suggestion/AI 会话等)字段未定不预生成。
