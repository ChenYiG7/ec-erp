# API 模型 CQRS 分包(同日晚):dto/vo/model 平铺统一为 `request/command`(写入参

2026-09-03 API 模型 CQRS 分包(同日晚):dto/vo/model 平铺统一为 `request/command`(写入参 SaveRequest)+ `request/query`(读入参 Query)+ `response`(出参 Response,原 XxxVO 更名);shop/system 两模块存量已迁,CodeGenerator 八件套模板与 docs/07 §1 同步改版,新生成域直接产新结构。
