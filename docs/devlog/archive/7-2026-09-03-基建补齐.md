# 基建补齐:①时间戳两列规约落地(19 表 created_at/updated_at 全覆盖,开发库 ALTER 已执行

2026-09-03 基建补齐:①时间戳两列规约落地(19 表 created_at/updated_at 全覆盖,开发库 ALTER 已执行,清单存档 TODO.md #7);②全部业务模块 pom 对齐标准栈(web+validation+MP 三件套+starter-test);③spring-boot-starter-validation 引入(@Valid 随业务约束注解逐步启用);④分页统一走 `query.pageSize()` 钳制版(≤500);⑤连接与密钥全部外置:application.yml 零明文,本地值在 git-ignored `local.properties`(键=环境变量名,env 优先级更高;模板 local.properties.example;文件与变量皆缺时启动即失败);⑥API 模型收口定版(docs/07 §1:入参 XxxQuery/XxxSaveRequest、出参 XxxResponse,entity 不出 Service 层,显式 from/toEntity 禁反射拷贝;GlobalExceptionHandler 兜 BindException→400;shop 域试点完成,erp-codegen 升级八件套,存量域翻新按 TODO.md #8 逐域走)。
