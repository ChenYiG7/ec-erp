# SKU 匹配落地(#5 主体):`ShopProductService.saveUnifiedProduct` = li

2026-09-04 SKU 匹配落地(#5 主体):`ShopProductService.saveUnifiedProduct` = listing 同步唯一写入口(主表+SKU 行双 upsert,**绑定列不在更新列——同步永不覆盖 product_id/sku_id/match_status**);自动匹配三段式收口 erp-api `ProductPullJob`(PRODUCT 独立游标默认 1 小时):同步落库 → `ProductService.mapSkuCodesToIds` 精确匹配(seller_sku==sku_code)→ `ShopProductSkuService.autoMatch` 回填 match_status=1(仅 NULL 行,人工绑定不被覆盖);订单翻译已在 #4。删除 SKU/SPU 引用校验因跨 shop/inventory/order 三模块待人工拍板方案(erp-api 编排 or 接口模块),TODO(#5) 留在 ProductService/bind。
