# 删除引用校验 + bind 存在性收口(#5 遗留 + #3 店铺删除,拍板:接口模块):新增 **erp-contra

2026-09-04 删除引用校验 + bind 存在性收口(#5 遗留 + #3 店铺删除,拍板:接口模块):新增 **erp-contract** 契约模块(零依赖零接口实现,docs/07 §2.2)——GoodsReferenceApi.countSkuRefs(绑定/库存/订单明细/采购明细四域合计)+ countProductListingRefs(SPU 的 listing 引用)、GoodsSkuApi.existsSku、ShopReferenceApi.countOrderRefs/countAftersaleRefs(店铺删除,listing/pull_log 域内直查);erp-goods/erp-shop 只依赖接口,GoodsReferenceApiImpl/GoodsSkuApiImpl/ShopReferenceApiImpl 收口 erp-api,取数走归属域 Service 的 count 方法;`ProductService.deleteSku/deleteProduct` 有引用即抛 BusinessException 禁删、`ShopService.deleteShop` 同、`ShopProductSkuService.bind` 查无 sku_id 禁绑。⚠️ 坑补充:MP Lambda wrapper 的 `.in()`/`.set()` 与 `.select()` 同为急切解析列元数据,纯 Mockito 单测环境不可直测(报 can not find lambda cache),计数方法正确性由汇总层 GoodsReferenceApiImplTest/ShopReferenceApiImplTest + 运行时覆盖。
