# 售后状态机落地(#12 主体,拍板:草案 7 态扩 8 态,新增 **RETURN_RECEIVED已收退件**;已建库

2026-09-04 售后状态机落地(#12 主体,拍板:草案 7 态扩 8 态,新增 **RETURN_RECEIVED已收退件**;已建库手工 ALTER COMMENT 清单存 TODO.md #12):`AftersaleConsts` 词表 + `AftersaleOrderMapper.casStatus/refundOrder` @Update 条件更新即守卫(docs/07 §6.3),**同一 UPDATE 原子回填 result**;五动作人工处理入口 `POST /api/aftersale/orders/{id}/agree|reject|receive-return|refund|complete`——agree 按 type 分流(仅退款/补发→APPROVED,退货退款/换货→RETURNING,未知 type 抛),reject result 必填,refund 前置白名单 `status IN (APPROVED, RETURN_RECEIVED)` 即类型血缘(退货类强制已收退件),complete REFUNDED→COMPLETED 收口终态;处理接口不限 admin(对齐 #11 发货先例),单步流转不加事务(#10 audit 先例);Query 补 shopId/status/type/orderId 过滤(idx_status/idx_order 预置);CANCELLED 预留平台同步撤单(无人工入口),同步 upsert(TODO(#12) 槽位仍在 AftersaleOrderService)与退货入库 IN_RETURN(需表结构扩容走 add-table)待后续。单测 12 个全绿。
