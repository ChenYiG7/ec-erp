# 03 数据库设计(核心表)

库名 `erp`,字符集 utf8mb4。

> **本文档已与可执行脚本 `docs/sql/01_schema_init.sql`(v0.1)对齐(2026-09-02;2026-09-03 时间戳规约落地:11 张表补齐 created_at/updated_at,已建库的 ALTER 见 TODO.md #7;2026-09-04 二期采购/发货/售后 8 表定稿进脚本并随 #10/#11/#12 激活;2026-09-06 三期 AI 辅助 3 表定稿进脚本并随 #6 AI 地基激活)**:一期核心表以脚本为准;
> 二期采购/发货/售后表已定稿(§5);三期 AI 辅助表已定稿(§7),结算/广告表仍是草案随模块开工再落。所有表带 `created_at/updated_at`,
> 逻辑删除已落地(TODO#7,2026-09-08):人工域 11 表(sys_user/sys_role/sys_menu/sys_dict/brand/product/product_category/product_sku/shop/warehouse/supplier)
> 带 `deleted BIGINT NOT NULL DEFAULT 0` + 实体 `@TableLogic(value="0", delval="id")`(删时置主键 id),
> 业务唯一键重建为含 deleted(删后同键可重建);平台同步正本/单据/流水快照/AI/settlement 域保持物理删除(拍板见 TODO#7)。
> 建表/索引/SQL/ORM 规范以 docs/07 §6 为准——基线《阿里巴巴Java开发手册》MySQL 规约 + 项目偏差声明
> (金额一律 DECIMAL(12,4) 而非最小货币单位整型;时间列为 created_at/updated_at 而非 create_time/update_time,均已写明理由)。

## 1. 系统与平台

```sql
-- 店铺(平台中心心脏)
CREATE TABLE shop (
  id              BIGINT AUTO_INCREMENT PRIMARY KEY,
  merchant_id     BIGINT NOT NULL DEFAULT 1 COMMENT '商户ID,一期固定1',
  platform        VARCHAR(32) NOT NULL COMMENT 'PlatformType 枚举名:TAOBAO/AMAZON/...',
  shop_name       VARCHAR(128) NOT NULL,
  seller_id       VARCHAR(64)  COMMENT '平台店铺唯一标识(如亚马逊 SellerId)',
  app_key         VARCHAR(128) COMMENT '平台应用Key,明文存(非机密,参与签名)',
  app_secret      VARCHAR(512) COMMENT 'AES-GCM 密文(密钥 ERP_TOKEN_KEY)',
  access_token    VARCHAR(2048) COMMENT 'AES-GCM 密文(密钥 ERP_TOKEN_KEY)',
  refresh_token   VARCHAR(2048) COMMENT 'AES-GCM 密文(密钥 ERP_TOKEN_KEY)',
  token_expire_at DATETIME,
  status          TINYINT NOT NULL DEFAULT 1 COMMENT '1启用 0停用',
  UNIQUE KEY uk_platform_seller (platform, seller_id)   -- 防重复建店;seller_id 为 NULL 时不约束
) COMMENT '店铺';

-- 平台拉取日志(排障与增量游标依据)
CREATE TABLE pull_log (
  id           BIGINT AUTO_INCREMENT PRIMARY KEY,
  shop_id      BIGINT NOT NULL,
  data_type    VARCHAR(32) NOT NULL COMMENT 'ORDER/PRODUCT/REFUND',
  window_start DATETIME NOT NULL,
  window_end   DATETIME NOT NULL,
  pulled_count INT NOT NULL DEFAULT 0,
  success      TINYINT NOT NULL DEFAULT 1,
  error_msg    TEXT,
  duration_ms  INT COMMENT '本次拉取耗时(毫秒),观测慢店铺/慢接口',
  pull_way     VARCHAR(16) COMMENT '触发方式:JOB定时/MANUAL手动',
  KEY idx_shop_type_time (shop_id, data_type, window_end)
) COMMENT '平台拉取日志(游标=最近一次成功记录的 window_end,左叠5分钟)';

-- 已建:sys_user / sys_role / sys_dict;
-- RBAC 三表(TODO#1 已落地,DDL 见脚本):sys_menu(树形菜单/权限,perm_key 预留按钮级鉴权)、
--   sys_user_role(用户-角色,联合主键)、sys_role_menu(角色-菜单,联合主键)。
--   初始化:admin 角色 + admin 用户(默认密码 admin@123,BCrypt)+ 一级菜单骨架,INSERT IGNORE 幂等

-- 系统参数(TODO#18,2026-09-07 拍板:大模型/预警/AI 工作流等启动后可变项前端可配):
--   键值对单表 + GROUP 分组(前端按组渲染面板);值一律 VARCHAR(1024) 文本存储,
--   数字/布尔由消费侧解析解析失败回落默认值(禁浮点/金额键,金额走 DECIMAL 表列);config_key 唯一键幂等;
--   DB 无行 = 走代码默认值(yml 仍可覆盖默认),改动即时生效(本进程内持 Provider 单例,每轮取值直查/带短缓存)。
--   凭证类(AI api-key/平台密钥)禁入本表——安全红线 docs/07 §7,凭证只走环境变量/local.properties

-- 站内通知(TODO#14;系统写入表:告警由任务扇出写入,对外只读查询 + 用户已读状态接口;
--   已读状态用 read_status 命名规避 is_ 前缀列,与 match_status/success 同风格)
CREATE TABLE sys_notification (
  id           BIGINT AUTO_INCREMENT PRIMARY KEY,
  user_id      BIGINT NOT NULL COMMENT '接收用户ID(sys_user.id),写侧扇出到全部启用用户',
  title        VARCHAR(128) NOT NULL COMMENT '通知标题',
  content      VARCHAR(1024) COMMENT '通知内容(写侧截断)',
  notify_type  VARCHAR(32) NOT NULL COMMENT '通知类型:PULL_FAIL=拉单连续失败告警',
  biz_type     VARCHAR(32) COMMENT '关联业务类型:SHOP等',
  biz_id       BIGINT COMMENT '关联业务ID(如店铺ID)',
  read_status  TINYINT NOT NULL DEFAULT 0 COMMENT '已读状态:0未读 1已读',
  read_at      DATETIME COMMENT '已读时间',
  KEY idx_user_read (user_id, read_status),
  KEY idx_created (created_at)
) COMMENT '站内通知(系统告警扇出+用户已读状态)';

-- 系统参数(TODO#18;系统管理域:启动后可变项的运行时快照,前端系统设置页编辑)
CREATE TABLE sys_config (
  id           BIGINT AUTO_INCREMENT PRIMARY KEY,
  config_group VARCHAR(32)  NOT NULL COMMENT '参数组:AI=大模型与AI工作流 ALERT=库存预警 CHAT=AI对话 AGENT=AI智能体 SALES=销量统计',
  config_key   VARCHAR(64)  NOT NULL COMMENT '参数键(与 yml relaxed-binding 键同名,如 erp.ai.replenish.low-stock-threshold),唯一',
  config_value VARCHAR(1024) COMMENT '参数值(文本存储,数字/布尔由消费侧解析;凭证类禁入本表)',
  remark       VARCHAR(255) COMMENT '参数说明(前端表单旁展示)',
  created_at   DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  updated_at   DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  UNIQUE KEY uk_config_key (config_key)
) COMMENT '系统参数(启动后可变项键值对:大模型/预警阈值/AI工作流参数等;组=AI/ALERT/CHAT/AGENT/SALES)';
```

## 2. 商品与 SKU 绑定(系统心脏)

```sql
CREATE TABLE product (
  id          BIGINT AUTO_INCREMENT PRIMARY KEY,
  spu_code    VARCHAR(64) NOT NULL UNIQUE COMMENT '内部SPU编码',
  name        VARCHAR(255) NOT NULL,
  category_id BIGINT, brand_id BIGINT,
  attrs_json  JSON COMMENT '销售属性',
  status      TINYINT NOT NULL DEFAULT 1
) COMMENT 'ERP商品SPU';

CREATE TABLE product_sku (
  id             BIGINT AUTO_INCREMENT PRIMARY KEY,
  product_id     BIGINT NOT NULL,
  sku_code       VARCHAR(64) NOT NULL UNIQUE COMMENT 'ERP SKU 编码',
  barcode        VARCHAR(64) COMMENT '条码/EAN/UPC',
  attrs_json     JSON COMMENT '规格(颜色/尺码)',
  cost_price     DECIMAL(12,4) COMMENT '成本价(本位币)',
  weight_g       INT,
  hs_code        VARCHAR(32) COMMENT '跨境:海关编码',
  declared_value DECIMAL(12,4) COMMENT '跨境:申报价值',
  battery        TINYINT NOT NULL DEFAULT 0 COMMENT '跨境:含电标记(物流属性)',
  status         TINYINT NOT NULL DEFAULT 1
) COMMENT 'ERP商品SKU';
-- 长/宽/高尺寸列二期按物流计费需要再加

-- 店铺商品(平台 listing,拉取生成;先拉取后绑定,故 product_id 可空)
CREATE TABLE shop_product (
  id                  BIGINT AUTO_INCREMENT PRIMARY KEY,
  shop_id             BIGINT NOT NULL,
  platform_product_id VARCHAR(64) NOT NULL,
  platform_sku_id     VARCHAR(64) COMMENT '平台商品级SKU标识(部分平台有)',
  product_id          BIGINT COMMENT '绑定的 product.id,NULL=未绑定',
  listing_status      VARCHAR(32),
  last_sync_at        DATETIME,
  UNIQUE KEY uk_shop_platform_product (shop_id, platform_product_id),
  KEY idx_product (product_id)
) COMMENT '店铺商品(平台listing ↔ 内部SPU)';

-- SKU 映射绑定表(心脏表,订单/库存/报表都经它换算成内部 sku_id)
CREATE TABLE shop_product_sku (
  id              BIGINT AUTO_INCREMENT PRIMARY KEY,
  shop_product_id BIGINT NOT NULL,
  seller_sku      VARCHAR(128) NOT NULL COMMENT '平台侧商家编码(Amazon: seller-sku;国内: sku_id),自动匹配依据',
  sku_id          BIGINT COMMENT '绑定的 product_sku.id,NULL=未绑定(进待匹配列表)★',
  quantity        INT COMMENT '平台侧可售数量快照',
  price           DECIMAL(12,4),
  currency        CHAR(3),
  match_status    TINYINT NOT NULL DEFAULT 0 COMMENT '0待匹配(sku_id为NULL) 1商家编码自动 2人工',
  UNIQUE KEY uk_shop_seller_sku (shop_product_id, seller_sku),
  KEY idx_sku (sku_id)
) COMMENT 'SKU映射绑定(系统心脏)';
```

> 匹配流程(对应 TODO.md #5):拉取 listing → `seller_sku` 与 `product_sku.sku_code` 精确匹配自动绑定
> (`match_status=1`);匹配不中则 `sku_id=NULL / match_status=0` 照常入库,进"待匹配"页人工绑定(`match_status=2`)。

## 3. 订单

```sql
CREATE TABLE shop_order (
  id                  BIGINT AUTO_INCREMENT PRIMARY KEY,
  shop_id             BIGINT NOT NULL,
  platform            VARCHAR(32) NOT NULL,
  platform_order_id   VARCHAR(64) NOT NULL,
  order_status        VARCHAR(32) NOT NULL COMMENT '统一状态:WAIT_PAY/WAIT_SHIP/SHIPPED/COMPLETED/CANCELLED/CLOSED',
  fulfillment_channel VARCHAR(32) NOT NULL DEFAULT 'SELF_FULFILL' COMMENT 'SELF_FULFILL/FBA/OVERSEAS_WAREHOUSE',
  order_time          DATETIME NOT NULL,
  paid_time           DATETIME,
  buyer_note          VARCHAR(512),
  -- 收件人直接平铺成列(国内整串地址,跨境需 州/邮编 分列检索)
  receiver_name VARCHAR(64), receiver_phone VARCHAR(32), receiver_country VARCHAR(8),
  receiver_state VARCHAR(64), receiver_city VARCHAR(64),
  receiver_address VARCHAR(512), receiver_zip VARCHAR(32),
  currency      CHAR(3) NOT NULL,
  exchange_rate DECIMAL(12,8) NOT NULL DEFAULT 1 COMMENT '下单日汇率快照',
  order_amount    DECIMAL(12,4) NOT NULL DEFAULT 0,
  shipping_fee    DECIMAL(12,4) NOT NULL DEFAULT 0,
  discount_amount DECIMAL(12,4) NOT NULL DEFAULT 0,
  raw_json      JSON COMMENT '平台原始报文,翻译出错可回溯重放',
  UNIQUE KEY uk_shop_platform_order (shop_id, platform_order_id),   -- 幂等关键
  KEY idx_order_time (order_time),
  KEY idx_status (order_status)
) COMMENT '平台订单(统一订单)';
-- 发货截止时间 ship_by_time(国内平台考核)已于 #11 随发货单模块加列(2026-09-04,见 §5 delivery_order)

CREATE TABLE shop_order_item (
  id              BIGINT AUTO_INCREMENT PRIMARY KEY,
  order_id        BIGINT NOT NULL,
  platform_order_item_id VARCHAR(64),
  shop_product_id BIGINT,
  shop_product_sku_id BIGINT,
  sku_id          BIGINT COMMENT '落库时按绑定关系翻译出的内部SKU,未绑定则 NULL ★',
  platform_sku    VARCHAR(128),
  product_name    VARCHAR(255),
  quantity        INT NOT NULL,
  unit_price      DECIMAL(12,4) NOT NULL DEFAULT 0,
  item_amount     DECIMAL(12,4) NOT NULL DEFAULT 0,
  currency        CHAR(3) NOT NULL,
  KEY idx_order (order_id),
  KEY idx_sku (sku_id)
) COMMENT '订单明细';
-- 明细级退款状态 refund_status:售后模块已激活(#12)暂未加列(2026-09-04),随平台售后同步 upsert(TODO.md #12 遗留)时再评估
```

> FBA 订单:正常入库,`fulfillment_channel=FBA`,不产生发货单,只参与对账与报表。

## 4. 仓库与库存

```sql
CREATE TABLE warehouse (
  id      BIGINT AUTO_INCREMENT PRIMARY KEY,
  wh_name VARCHAR(128) NOT NULL,
  wh_type VARCHAR(32) NOT NULL DEFAULT 'SELF' COMMENT 'SELF自仓/FBA/OVERSEAS海外仓/VIRTUAL虚拟仓',
  country VARCHAR(8),
  address VARCHAR(512),
  status  TINYINT NOT NULL DEFAULT 1
) COMMENT '仓库';

CREATE TABLE inventory (
  id            BIGINT AUTO_INCREMENT PRIMARY KEY,
  sku_id        BIGINT NOT NULL,
  warehouse_id  BIGINT NOT NULL,
  qty_on_hand   INT NOT NULL DEFAULT 0 COMMENT '在库',
  qty_locked    INT NOT NULL DEFAULT 0 COMMENT '占用(发货单占用未发货:建单占用/取消·删除·改单释放,#11)',
  qty_transit   INT NOT NULL DEFAULT 0 COMMENT '在途(采购审核占用未入库:审核占/关闭释放,#10)',
  qty_available INT NOT NULL DEFAULT 0 COMMENT '可用=在库-占用,由 InventoryService 同事务维护,禁止旁路 update',
  UNIQUE KEY uk_sku_wh (sku_id, warehouse_id)
) COMMENT '库存(分仓,只能经 inventory_flow 变动)';
-- SKU 级安全库存(safety_stock)二期随"安全库存预警"功能加列

CREATE TABLE inventory_flow (
  id           BIGINT AUTO_INCREMENT PRIMARY KEY,
  sku_id       BIGINT NOT NULL,
  warehouse_id BIGINT NOT NULL,
  flow_type    VARCHAR(32) NOT NULL COMMENT 'IN_PURCHASE/OUT_SHIP/IN_RETURN/ADJUST/TRANSFER_OUT/TRANSFER_IN/IN_TRANSIT/LOCK_SHIP(#7 2026-09-06 八值)',
  quantity     INT NOT NULL COMMENT '正负数',
  before_qty   INT NOT NULL, after_qty INT NOT NULL,
  biz_type     VARCHAR(32) COMMENT '关联业务类型',
  biz_id       BIGINT COMMENT '关联单据ID',
  unit_cost    DECIMAL(18,8) NULL COMMENT '动账单价快照(CNY,移动加权 #19③)',
  cost_amount  DECIMAL(18,2) NULL COMMENT '动账成本额(CNY,带符号)',
  created_by   BIGINT,
  KEY idx_sku_time (sku_id, created_at)
) COMMENT '库存流水(只增不改,与库存变更同事务写入)';

-- SKU 移动加权成本账(#19③ 利润核算 V1 2026-09-08 定稿,docs/02 §14 成本计价拍板"先移动加权")
CREATE TABLE sku_cost_state (
  id           BIGINT AUTO_INCREMENT PRIMARY KEY,
  sku_id       BIGINT NOT NULL,
  total_qty    INT NOT NULL DEFAULT 0 COMMENT '账本结存数量(全局跨仓,加权分母)',
  total_amount DECIMAL(18,2) NOT NULL DEFAULT 0 COMMENT '账本结存金额(CNY,加权分子)',
  avg_cost     DECIMAL(18,8) NOT NULL DEFAULT 0 COMMENT '当前移动加权单价(CNY);清零保留末次价作暂估基准',
  updated_at   DATETIME,
  UNIQUE KEY uk_sku (sku_id)
) COMMENT 'SKU移动加权成本账(出库成本事实源:OUT_SHIP 行 unit_cost 快照落 inventory_flow)';
```

> 库存变更必须走统一入口(`InventoryService.change`,TODO.md #7),同事务写 flow,禁止直接 update 数量列。
> flow_type 列语义矩阵(#7 2026-09-06 差异化落地,收口 InventoryService.FlowOps,词表三方同步:
> InventoryConsts / 本文档 / 01_schema_init.sql):
>
> | flow_type | 业务动作 | qty_on_hand | qty_locked | qty_transit | qty_available | 守卫(WHERE) |
> |---|---|---|---|---|---|---|
> | IN_TRANSIT | 采购审核占在途(+)/关闭释放(-) | – | – | +q | – | 无 |
> | IN_PURCHASE | 入库核销(转可用) | +q | – | -q | +q | 在途 ≥ q |
> | IN_RETURN | 售后退货入库 | +q | – | – | +q | 可用+q ≥ 0 |
> | ADJUST | 人工调整(±) | +q | – | – | +q | 可用+q ≥ 0 |
> | TRANSFER_OUT/IN | 调拨两腿(InventoryService.transfer 组合) | +q | – | – | +q | 可用+q ≥ 0 |
> | LOCK_SHIP | 发货建单占用(+)/取消·删除·改单释放(-) | – | +q | – | -q | 可用-q ≥ 0 |
> | OUT_SHIP | 发货出库(核销占用,q<0) | +q | +q | – | –(不变) | 在库/占用 ≥ \|q\| |
>
> 不变量 qty_available = qty_on_hand − qty_locked 全类型成立;IN_TRANSIT/OUT_SHIP 流水 before=after(不动可用)属正常。
> 成本账口径(#19③ 2026-09-08 定稿):`sku_cost_state` 全局跨仓移动加权,动账同事务维护(铁律 4 唯一入口 change,
> 出库结转 SELECT FOR UPDATE state 行串行化,锁序 inventory 行 → state 行单向无死锁)。进账矩阵:
> IN_PURCHASE 进(采购单价,缺价按当时加权暂估禁猜价)/OUT_SHIP 结转(当时加权价,unit_cost 快照落流水)/
> IN_RETURN·ADJUST 按当时加权价进(不动加权)/IN_TRANSIT·LOCK_SHIP·TRANSFER_OUT·TRANSFER_IN 不进成本账(NULL)。
> 结存金额可由 flow 成本列逐笔重放校验(V2 对账)。
> 三期评估:增加残品维度(qty_defective,海外仓退件/FBA 退货场景;启航云仓 12 种库存状态太重,取良品/残品两态即可),
> 另加日快照表 inventory_snapshot_daily,报表/周转分析不再跑流水全表。

## 5. 采购 / 发货 / 售后(二期,2026-09-04 定稿进脚本并激活 #10/#11/#12)

```sql
supplier(id, name UNIQUE, contact, phone, settle_type, remark)
purchase_order(id, po_no UNIQUE, supplier_id, warehouse_id, status[草稿/已审核/部分入库/已入库/已关闭], total_amount, remark)
purchase_order_item(id, po_id, sku_id, quantity, arrived_qty, purchase_price)
purchase_inbound(id, inbound_no UNIQUE, po_id, warehouse_id, status[PENDING待入库/RECEIVED已入库/CANCELLED已取消])  -- 确认入库 → 逐行写 inventory + flow(flow_type=IN_PURCHASE)
purchase_inbound_item(id, inbound_id, po_item_id, sku_id, inbound_qty)  -- 入库明细(#10 激活补,2026-09-04 拍板:支持多次部分收货与凭证留痕)

delivery_order(id, order_id, shop_id, warehouse_id, type[MANUAL/WAYBILL/SUPPLIER/FBA/OVERSEAS], status[PENDING/SHIPPED/DELIVERED/CANCELLED], ship_by_time, logistics_company, tracking_no UNIQUE, waybill_url, shipped_at, created_by)  -- 发货单(#11 激活扩列 2026-09-04:warehouse_id 出库仓扣库存必需;ship_by_time 承诺发货时限;created_by 对齐 purchase_inbound)
delivery_order_item(id, delivery_id, order_item_id, sku_id, ship_qty)  -- 发货明细(#11 激活补,2026-09-04 拍板:支持多次部分发货与凭证留痕;发货进度事实源——不可存 shop_order_item,拉单 replaceItems 先删后插会冲掉;仅 sku_id 已绑定的订单行参与发货与发足判定)
aftersale_order(id, shop_id, platform_refund_id UNIQUE, order_id, warehouse_id, type[REFUND_ONLY/RETURN_REFUND/EXCHANGE/RESEND], status[8态见01_schema], refund_amount, currency, reason, result)  -- 售后单(#12 激活扩列 2026-09-04:warehouse_id 退货入库仓,收退件时必填回填,同步落库时 NULL)
aftersale_return_item(id, aftersale_id, order_item_id, sku_id, return_qty)  -- 售后退货明细(#12 激活补,2026-09-04 拍板:receive-return 人工录入实收,同事务 IN_RETURN 动账凭证;SKU 服务端按订单行回填,仅 sku_id 已绑定行可退,归属/数量预校验防超退)
```

## 6. 财务 / 广告(三期)

> settlement 三件套已按本节定稿进 `01_schema_init.sql`(2026-09-08 随 #19 settlement 主线立项,
> CREATE IF NOT EXISTS 幂等,已建库重跑脚本即补建);`ad_report_daily` 留草案随 erp-ads 广告数据面激活时建表。

```sql
settlement_report(id, shop_id, settlement_id[平台结算批次号,幂等键 uk_shop_settlement],
                  period_start, period_end, currency,
                  total_amount[报告头TotalAmount原值,勾稽基准=Σ明细], fee_amount[Σ负项绝对值],
                  transfer_amount[Σ打款明细;预留金随真凭证实测校准], raw_file_url, status[PARSED勾稽平/FAILED可重拉],
                  pulled_at, created_at, updated_at,
                  KEY idx_shop_period (shop_id, period_start))
settlement_detail(id, report_id, shop_id[冗余], order_id, order_item_id[平台订单行号,SKU级利润归集键],
                  sku[平台原文,禁解析器猜映射], fee_type[解析器归一只加不改:SALE/REFUND/COMMISSION/FBA_FEE/STORAGE/ADVERTISING/TRANSFER/OTHER],
                  amount[报告原值带符号,禁取绝对值], posted_at, created_at, updated_at,
                  KEY idx_report/idx_shop_item/idx_posted)
exchange_rate(id, currency, rate[1 currency = rate CNY,本位币V1固定CNY], quoted_at, source[MANUAL/API],
              KEY idx_currency_quoted (currency, quoted_at))  -- 汇率是快照不是现值,折算按业务日回溯取最近报价
ad_report_daily(id, shop_id, stat_date, campaign_id, impressions, clicks, ctr, spend, sales, acos)  -- 按 shop+date+campaign 唯一;留草案
```

### 6.1 利润核算 V1(#19③ 2026-09-08,实时销售利润先行,三口径第一层 docs/02 §14)

- **口径**:订单行粒度,利润 = 售价(CNY) − 出库成本(CNY) − 平台佣金(CNY);已支付态三态(WAIT_SHIP/SHIPPED/COMPLETED)
  同销量日表口径;**不落库实时算**(订单面小时级新鲜度天然满足,V2 周期口径才落库校准)。
- **售价折算**:item_amount × 汇率,汇率按**下单日(so.order_time)回溯 exchange_rate** 取 quoted_at<=order_time 最近一条
  (禁取表内最新;CNY 短路=1;无报价→本位币列为 NULL,禁猜);shop_order.exchange_rate 落库快照仅展示,利润面统一回溯口径。
- **成本**:OUT_SHIP 行 unit_cost 快照(移动加权,见 §4 sku_cost_state),经 delivery_order_item(biz_id=delivery_id)
  按 order_item_id 聚合;未出库行成本 NULL(前端"未发货")。
- **佣金**:settlement_detail fee_type=COMMISSION 按 shop_id+order_item_id(=platform_order_item_id)聚合;
  无结算数据(报告未拉/行无平台行号)→ NULL(前端"待结算"),V1 禁费率猜算,周期口径(②数据)到位后差值校准。

定稿偏差(相对首轮草案):结算单 `period` 拆 `period_start/period_end` 两列、补 `settlement_id` 幂等键
(Amazon SettlementId,同店铺唯一,重拉 upsert 防重复入库);明细补 `shop_id` 冗余(同事务写入免联查)、
`order_item_id`(SKU 级利润归集键,对应 shop_order_item.platform_order_item_id)与 `sku` 平台原文列
(本地映射走 #5 绑定关系联查,解析器禁猜);金额全带符号存报告原值(Σ明细=报告头 TotalAmount 即勾稽平,
否则整单 FAILED 可重拉,禁静默截断);明细行级不设唯一键(平台允许多行同键事件),幂等收口报告级——
重拉按 report 先删后插(同 #4 拉单明细纪律);汇率补 `source` 与回溯口径拍板(快照≠现值)。

### 6.2 退款勾稽(#19④ 2026-09-08,#12 遗留"退款金额与财务勾稽"收口)

- **口径**:店铺 + 平台订单号聚合比对(结算 REFUND 行无退款单 ID,逐单勾稽不可行;售后侧经 shop_order
  翻译 platform_order_id,同订单多售后单/部分退款自然 SUM)。售后侧 = 已退款终态(REFUNDED/COMPLETED)
  且 refund_amount 非空;结算侧 = PARSED 报告 REFUND 行(FAILED 明细是待校准暂存态不入比对)。
  售后恒正(绝对值口径)/结算 Σ(−amount) 转正同向。
- **差异判定三类**:`AMOUNT_MISMATCH`(双侧有行且 |差| > 0.01,容差=结算列精度防尾差误报)/
  `MISSING_IN_SETTLEMENT`(售后已退款而 PARSED 报告无该订单 REFUND 行)/`CURRENCY_MISMATCH`
  (任一侧同订单多币种或两侧币种不同,禁混币计算)。勾稽平(|差| ≤ 容差)不告警。
- **落位**:erp-finance RefundReconciliationService 纯读侧(零 DDL,跨域只读 join 同 ProfitQueryMapper
  先例),erp-api RefundReconciliationJob 每日一扫 → 静默期去重 → #14 站内通知扇出
  (notify_type=REFUND_DIFF,聚合单条明细列 topN,模式同 #6 预警)。开关 erp.finance.refund-reconciliation.*。
- **已知边界**(真凭证实测后评估):结算 REFUND 行按 #19② 拍板含退款负佣金行,售后 refund_amount 为
  Principal 本金口径,两侧或存佣金级系统性偏差——差异告警带双侧金额供人工判读,translator 归一
  校准时随 #19② 一并定版。

## 7. AI 辅助表(三期 2026-09-06 定稿进脚本,随 #6 AI 地基激活)

> 原则:**AI 永不直接写业务表**。产出一律落建议表,人工确认后走正常业务接口;对话落库可审计。
> `ai_suggestion`/`ai_chat_session`/`ai_chat_message` 三表已按本节定稿进 `01_schema_init.sql`
> (CREATE IF NOT EXISTS 幂等,已建库重跑脚本即补建);`inventory_snapshot_daily` 留草案随库存预警切片落地。

```sql
ai_suggestion(id, suggestion_type[REPLENISH补货/PRICING定价/ANOMALY异常/COPYWRITING文案],
              shop_id, sku_id, ref_type, ref_id, payload_json JSON, summary,
              risk_level[LOW/MID/HIGH], status[0待确认/1已采纳/2已忽略],
              confirmed_by, confirmed_at, created_at, updated_at,
              KEY idx_type_status (suggestion_type, status), KEY idx_sku (sku_id))
ai_chat_session(id, user_id, title, source[CHAT对话/AGENT智能体,四期 agent/ 2026-09-07 加列], created_at, updated_at,
              KEY idx_user_updated (user_id, updated_at))
ai_chat_message(id, session_id, role[USER/AI/TOOL], content MEDIUMTEXT,
                tool_name, prompt_tokens, completion_tokens, created_at,
                KEY idx_session_time (session_id, created_at))
inventory_snapshot_daily(id, stat_date, sku_id, warehouse_id,          -- 仍为草案
                qty_on_hand, qty_locked, qty_transit, qty_available,
                UNIQUE uk_sku_wh_date (sku_id, warehouse_id, stat_date))
```

定稿偏差(相对首轮草案):会话/消息补 `updated_at` 规约列与组合索引(我的会话按最近更新排序、消息按会话内时间序);
建议表补 `idx_sku` 与 `updated_at`;`summary` 收 NOT NULL(LLM 结论一句话必回传);payload 用 JSON 型对齐 raw_json 先例。
`ai_suggestion` 状态推进 = 单条 cas 条件更新(0→1/0→2,WHERE status=0 即守卫),与业务单据状态机同款纪律(docs/07 §6.3)。

### 7.1 销量数据面 order_sales_daily(#6 2026-09-07 定稿进脚本,补货动销/滞销积压预警数据面)

> 口径拍板(2026-09-07):统计维度 = 支付日(DATE(paid_time))× SKU 合计购买数量;
> 范围 = 已支付态订单(WAIT_SHIP/SHIPPED/COMPLETED),WAIT_PAY 未付不计,CANCELLED/CLOSED 不计,
> 已支付后回转 CANCELLED 的单随 30 天重算窗口自动退出;未绑定 SKU(sku_id NULL)不统计(同"报表以已绑定为口径");
> V1 只落 qty_sold 数量维(现消费者仅动销/滞销/积压),金额维(选品/ACOS)随需要再加列(ALTER 登记 TODO);
> 归属 erp-order(源数据域),重算 = 每日窗口 upsert(uk_sku_date 幂等,覆盖状态回传修正,可安全重试),
> 调度在 erp-api SalesSnapshotJob(01:00,赶在补货工作流 02:00 前出当日数据);erp-ai 读侧走
> SalesQueryApi 只读契约(第五件,铁律 2),不做实时聚合查询——统计面就是权威读面。

```sql
order_sales_daily(id, stat_date, sku_id, qty_sold,
                  created_at, updated_at,
                  UNIQUE uk_sku_date (sku_id, stat_date), KEY idx_date (stat_date))
```

`inventory_snapshot_daily` 仍留草案:库存趋势/周转报表面与销量面相互独立,随报表切片另行落地。

> 读侧扩容(2026-09-08,补货算法 V2):SalesQueryApi 契约只加方法 `listDailyQtyBySku`(逐日序列
> Map<skuId, Map<date, qty>>;只回有记录 skuId,窗口内零销日不出现由调用方补 0)——需求波动 σ
> 需逐日样本,合计口径丢失波动信息;实现链 OrderSalesDailyService.listDailyQtyBySku +
> OrderSalesDailyMapper XML listQtySince(sku_id IN + stat_date range 走 uk_sku_date 前缀索引)。
> 表结构零变更(数量维已够 V2 公式:μ 日均/σ 样本标准差均在消费侧计算)。

### 7.2 AI 客服知识库 ai_kb_document / ai_kb_chunk(#6 RAG V1 2026-09-08 定稿进脚本)

> 向量库拍板(2026-09-08):Spring AI SimpleVectorStore(JSON 文件持久化,零新基建;后续换 pgvector/Redis
> 只换实现类);**向量不入库**——向量是索引派生物,本节两表存文本正本,索引文件丢失/换 embedding 模型时
> 按正本重嵌入重建(启动自动判定 + 手动端点双路径)。chunk id(String.valueOf)= 向量库文档 id,删除按此对齐。

```sql
ai_kb_document(id, title, source_type[UPLOAD文件/TEXT粘贴], file_name,
               char_count, chunk_count, status[READY可检索/FAILED向量化失败], uploaded_by,
               created_at, updated_at, KEY idx_created (created_at))
ai_kb_chunk(id, document_id, chunk_index, content TEXT, char_count, created_at,
            UNIQUE uk_doc_idx (document_id, chunk_index), KEY idx_doc (document_id))
```

## 设计要点

1. **幂等**:所有平台数据表以 `(shop_id, platform_xxx_id)` 唯一键兜底,拉单可安全重试;shop 表以 `(platform, seller_id)` 防重复建店;
2. **金额一律 DECIMAL(12,4),禁用 float/double**;汇率 DECIMAL(12,8);跨境金额同时存原币与经 `exchange_rate` 折算的本位币;
3. **绑定缺失兜底**:订单明细 `sku_id` 允许 NULL(未绑定照常入库),报表统计以已绑定为口径,后台提供"未绑定订单"告警;
4. **原始报文留档**:`raw_json` 保证字段翻译出错时可回溯重放;
5. 后续迁移注意:多商户(四期)给 shop/order/inventory 等表统一加 `merchant_id` 冗余列,一期先在 shop 表预留。

## 8. 报表读侧(#20/#21,2026-09-08 落地)

报表域(erp-report)与利润看板均为**零 DDL 纯读侧**,不新增表:

- 数据面:销售面 = `order_sales_daily`(销量日表,#6,支付日口径/已支付态三态/未绑定 SKU 不统计),
  库存面 = `inventory_snapshot_daily`(库存日快照,#6,快照不可回溯);
- 跨域只读聚合 XML join 商品/仓库翻译——**join 不滤已删**(TODO#7 拍板:历史销量/历史快照要能显示已删主数据名字);
- 利润趋势/排行复用 #19③ 实时利润装配管线(逐行补成本/佣金/汇率后内存分组),口径与 summarize 严格同源,
  不另写 SQL 折算(折算口径单点收口 docs/03 §6.1);
- 广告面 `ad_report_daily` 仍为草案,随 erp-ads 广告数据面激活(卡真凭证)。
